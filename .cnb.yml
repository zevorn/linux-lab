# Push pipeline: build and push Docker image on every push
main:
  push:
    - services:
        - docker
      stages:
        - name: build-and-push-image
          image: docker:latest
          script:
            - docker build -t linux-lab:latest .
            - docker tag linux-lab:latest registry.cnb.cool/gevico.online/linux-lab:latest
            - docker push registry.cnb.cool/gevico.online/linux-lab:latest

# Cloud IDE: build image from Dockerfile, use cached result on subsequent opens
$:
  vscode:
    - docker:
        build: Dockerfile
      services:
        - vscode
        - docker
      stages:
        - name: verify
          script:
            - qemu-system-arm --version
            - arm-linux-gnueabihf-gcc --version
            - dialog --version
